dist: trusty
sudo: false
language: c
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq valgrind

compiler:
  - clang
  - gcc

script:
 - cmake .
 - scan-build make
 - cmake --build . -- -j2
 - ctest -j2
 - ctest -T memcheck
